node_modules
.git
.gitignore
README.md
LICENSE
# CI download cache — only Dockerfile.ci needs .ci-download/shopizer-react/build/, all others exclude it
# (Dockerfile.ci explicitly COPYs from .ci-download/shopizer-react/build/ so it is NOT excluded above)
